name: gradle-build
run-name: gradle-build
on: [push]
jobs:
  Gradle-Run-Tests:
    runs-on: windows-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@HEAD
      - run: gradle build -i
